[phases.setup]
nixPkgs = ["python311", "postgresql_15", "libpq"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && gunicorn core.wsgi --log-file -"